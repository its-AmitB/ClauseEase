# AI_API_KEY: Optional API key for external AI service integration.
# Configure this if using an external AI service for enhanced features.
AI_API_KEY="YOUR_AI_API_KEY"

# APP_URL: The URL where this application is hosted.
# This may be injected at runtime based on the deployment environment.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="YOUR_APP_URL"
